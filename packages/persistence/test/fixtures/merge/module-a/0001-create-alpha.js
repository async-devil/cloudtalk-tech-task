// Fixture migration (unit tests only — never applied to a database).
export async function up() {
  // no-op: fixture for the folder-merge unit tests only
}
export async function down() {
  // no-op: fixture for the folder-merge unit tests only
}
